[pytest]
testpaths = tests
pythonpath = src
markers =
    slow: exercises the whole pipeline end to end; minutes rather than seconds
filterwarnings =
    ignore:.*encountered in matmul.*:RuntimeWarning
    ignore::UserWarning:sklearn.*
